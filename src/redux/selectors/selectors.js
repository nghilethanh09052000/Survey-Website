export const captchaMode = (state) => state.captchaReducerState;
export const questionMode = (state) =>state.questionReducerState
export const answerMode = (state) => state.answerReducerState
export const submitMode = (state) =>state.submitReducerState